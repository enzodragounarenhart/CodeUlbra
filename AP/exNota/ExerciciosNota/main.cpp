#include <stdio.h>
#include <ctype.h>

const int tam1 = 10, tam2 = 20;
int main()
{
    int v1[tam1], v2[tam1], v3[tam2] ;

    printf("Digite valores do vetor 1:");
    for (int i = 0; i < tam1; i++)
    {
        scanf("%i%*c", &v1[i]);
    }
    
    printf("Digite valores do vetor 1:");
    for (int j = 0; j < tam1; j++)
    {
        scanf("%i%*c", &v1[j]);
    }
    
    for (int k = 0; k < tam2; k++)
    {
        if(tam2)
    }

    return 0;
}
